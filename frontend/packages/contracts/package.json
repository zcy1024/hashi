{
  "name": "@hashi/contracts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "codegen": "sui-ts-codegen generate && pnpm fmt",
    "fmt": "prettier --write 'src/**/*.ts' '*.ts'",
    "check-fmt": "prettier --check 'src/**/*.ts' '*.ts'"
  },
  "dependencies": {
    "@mysten/sui": "^2.6.0"
  },
  "devDependencies": {
    "@mysten/codegen": "^0.8.2",
    "prettier": "^3.8.1",
    "typescript": "^5.9.3"
  }
}
